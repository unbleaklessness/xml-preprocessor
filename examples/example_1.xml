<mujoco>

  ${{x_pos = 0}}
  ${{y_pos = 1}}
  ${{z_pos = 2}}
  ${{c_radius = sqrt(z_pos)}}
  ${{c_size = y_pos + 1}}
  
	<worldbody>
		<body pos="${x_pos} ${y_pos} ${z_pos}">
			<geom type="cylinder" size="${(c_radius / 2) + 1} ${c_size}"/>
		</body>
	</worldbody>
</mujoco>